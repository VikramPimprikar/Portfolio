
  # Build app

  This is a code bundle for Build app. The original project is available at https://www.figma.com/design/dPjbcktnaSTgUIc6l7lzJ6/Build-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  